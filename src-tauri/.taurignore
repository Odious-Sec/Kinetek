# Files here do NOT trigger a Rust rebuild / app restart during `tauri dev`.
# (Tauri reads this like a .gitignore, relative to src-tauri/.)

# App icons — generated by `npx tauri icon`, irrelevant to the dev binary.
icons/

# Tauri-generated capability schemas, regenerated on every build.
gen/

# Build artifacts.
target/
